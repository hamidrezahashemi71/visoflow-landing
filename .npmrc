# Pin to the public npm registry so installs match the origin of pnpm-lock.yaml.
# This overrides any mirror the build environment injects, keeping the lockfile's
# tarball URLs consistent with the registry metadata (avoids supply-chain policy
# rejections during `pnpm install --frozen-lockfile`).
registry=https://registry.npmjs.org/
